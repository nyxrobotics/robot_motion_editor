from setuptools import find_packages
from setuptools import setup

setup(
    name='robot_motion_editor',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    zip_safe=True,
)
